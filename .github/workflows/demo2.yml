name: Deploy Website

on:
    push:
        branches:
            - master

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: Get code
              uses: actions/checkout@v3

            - name: Install dependencies
              run: npm install

            - name: Lint code
              run: npm run lint

            - name: Test code
              run: npm run test

    build:
        runs-on: ubuntu-latest
        steps:
            - name: Get code
              uses: actions/checkout@v3

            - name: Install dependencies
              run: npm install

            - name: Lint code
              run: npm run lint

            - name: Test code
              run: npm run test

            - name: Build code
              run: npm run build

    deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Deploy website
              run: echo "Deploying..."